requires visual studio 2019

python environment should be 3.7 (32-bit), requires:
- numpy
- PyAudio from https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
- built c++ project at PythonWrapper/OpenglBuild/

to compile install pyinstaller and use install.ps1 (assumes environment is called "env")

